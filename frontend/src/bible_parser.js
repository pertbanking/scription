// -*- encoding: utf-8 -*-
//
// (c) Joshua Petrin 2024. All rights reserved.
//
// File creation date: 15 November 2024
// File creator: Joshua Petrin

'use strict';

// Parse the XML Bibles in the 'bible' folder
const SRC_FOLDER = "bible/";

// class Bible {
//     constructor(version) {
//         this.data = 
//     }
// };

const importVerse = (book, chapter, verse) => {
    let entire_bible_str;
    // fs.readFile(SRC_FOLDER + "esv.xml", "utf8", (err, data) => {
    //     if (err) { throw err; }
    //     else { entire_bible_str = data; }
    // });

    // let desired_verse;

    // console.log(parseString);

    // parseString(entire_bible_str, (err, data) => {
    //     if (err) { throw err; }
    //     else { console.log(data); }
    // });
}

export default importVerse;
